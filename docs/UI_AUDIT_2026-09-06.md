# Interface audit - September 6, 2026

Browsed the live Briefing, Today, Events, Places, Communities, Updates, and
Research routes, inspected desktop screenshots, and exercised Research-to-place,
community-detail, place-events, and event-detail navigation. Verified the repair
locally against live Supabase data and inspected a 390px mobile screenshot.
This browser was signed out; personal account persistence was not tested.

## Repaired in this pass

- Hours proposal rendering crashed on `{closed: true}` days. This interrupted
  Briefing rendering and left misleading empty content and unknown agent states.
  Closed days now display correctly; absent times cannot throw this exception.
  Follow-up save repair: the database promoter also rejected closed days.
  Migration `20260906200542_allow_closed_days_in_hours_review.sql` now accepts
  explicit closed slots while rejecting malformed times. Tested the authenticated
  review RPC, all four remaining proposals, and invalid-time rejection inside
  rolled-back transactions. Failed saves also restore the original button label.
- Simple hours review cards now show their conflict explanation beside the
  proposed schedule, instead of requiring the user to infer why approval is needed.
- Community cards selected the earliest event sharing a source, even if it was
  unrelated to the Signal. Collector Legion showed a September 5 summary with
  an August 7 event. Links now require an explicit event relationship.
- A generic Instagram "Photo by" caption no longer counts as evidence of a
  personal social memory that keeps a past event in the digest.
- The header coverage ring was hard-coded to 49. It now uses the same visible
  reviewed-place count as Research (50/51, or 98%, in this signed-out check).
- Removed the remaining "Reviewed / partial" filter label.

## Next useful work, in priority order

1. Completed: shared Briefing/Today recommendation ranking now gives Commander
   meaningful format weight, retains limited-event interest, and weighs explicit
   event favorites above host favorites. Personal ratings remain directional;
   note length and click activity no longer imply endorsement in this ranking.
   Verified with actual saved Supabase preferences in an isolated browser:
   Finch Sunday Casual Play first, followed by Commander options, with the
   Collectors Lounge draft still in the top five. Hidden-event exclusion and
   note-length neutrality passed. No personal records were modified.
2. Completed: signed-in community monitoring now reads latest mapped surface
   checks through a minimal authenticated-only projection. Source research dates
   remain fallback rather than overriding newer successful monitoring checks.
3. Completed: JJ's expired July/August timing question removed; its assessment
   frames those announcements as historical evidence, not future promises.
   Four actual favorite stores checked; no expired dated questions found there.
4. Research copy/source drilldowns: static claims about poor non-Commander
   coverage sit beside 1,055 other-format records. Source-type bars are not
   clickable. Replace broad stale claims with current facts and useful links.
5. Completed: "Domains of the Alrisen D&D" was incorrectly tagged Magic by
   EventLink despite "Weekly D&D Session" in the source. Retired from the MTG
   calendar as D&D, with nine occurrences and sources retained. WPN adapter v5
   makes explicit RPG sessions ineligible, while preserving D&D-themed Magic.

Do not repeat a whole-interface audit to resume these items. Item 4 remains.
The original audit did not change canonical research or personal preferences.

## People & plans follow-through (September 6)

- Briefing shows three useful community conversations; Communities has the
  fuller catch-up. Shared Signal read state makes dismissal apply to both.
- Recent retained Discord findings now produce idempotent, seven-day community
  Signals for bounded newcomer, pod, draft, invitation, and meetup patterns.
  Six Signals were backfilled. This is a conservative routing bridge, not a
  general semantic surveyor or confirmation of an event or welcoming reply.
- Cards lead with the social topic and link to the source channel. Existing
  personal meetup Signals remain eligible under their existing freshness rules.
- Desktop/mobile browser checks passed: three cards, source link, detail
  drawer, shared local dismissal, reload persistence, no overflow or page errors.
  Network writes were blocked during testing; signed-in persistence uses the
  existing Signal state path, not a new storage mechanism.
- Monitoring projection exposes only source ID, check time, and disposition to
  authenticated users; anonymous access is revoked. Private notes are excluded.
